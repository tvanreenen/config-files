interface:
  display_name: "Design Architecture"
  short_description: "Design cohesive modules, interfaces, and seams"
  default_prompt: "Use $design-architecture to improve these module responsibilities, interfaces, seams, and dependencies for cohesion, testability, locality, and leverage."

policy:
  allow_implicit_invocation: true
