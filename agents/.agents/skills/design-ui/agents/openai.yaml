interface:
  display_name: "Design UI"
  short_description: "Design coherent, context-aware web interfaces"
  default_prompt: "Use $design-ui to design and implement this interface in alignment with the product, repository, and any established design system."

policy:
  allow_implicit_invocation: true
