interface:
  display_name: "Review Architecture"
  short_description: "Find and evaluate architectural improvements"
  default_prompt: "Use $review-architecture to review this codebase, identify evidence-backed architectural improvements, and recommend the strongest direction."

policy:
  allow_implicit_invocation: false
