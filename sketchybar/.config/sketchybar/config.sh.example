#!/bin/sh
# SketchyBar Configuration
# Copy this file to config.sh and fill in your actual values
# config.sh is gitignored and will not be committed

# Weather API GPS Coordinates
WEATHER_LATITUDE=25.99
WEATHER_LONGITUDE=-97.15

# API Monitor configuration (JSON format: service name -> URL)
# The key is the service name displayed in the popup menu
# The value is the URL to check for health/status
API_MONITOR_CONFIG='{
  "Service 1": "https://example.com/api/health",
  "Service 2": "https://example.com/api/status",
  "Service 3": "https://example.com/api/livez"
}'
