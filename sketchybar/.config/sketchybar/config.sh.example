#!/bin/sh
# SketchyBar Configuration
# Copy this file to config.sh and fill in your actual values
# config.sh is gitignored and will not be committed

# Weather API GPS Coordinates
WEATHER_LATITUDE=25.99
WEATHER_LONGITUDE=-97.15

# API Monitor URLs (shell array)
# Add as many URLs as you want, one per line
API_MONITOR_URLS=(
  "https://example.com/api/health"
  "https://example.com/api/status"
  "https://example.com/api/livez"
)
